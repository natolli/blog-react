import styled from "styled-components";
import { defaultTheme } from "../../utils/theme";

export const Form = styled.form`
  padding: 45px;
  width: 450px;
  border-right: 2px solid ${defaultTheme.secondaryColor};
  border-left: 2px solid ${defaultTheme.secondaryColor};
`;
